window.SIDEBAR_ITEMS = {"fn":[["blackman_iter","returns an iterator that yields the values for a blackman window of `size`"],["cosine_at","returns the value of the cosine window of `size` with the coefficients `a`, `b`, `c` and `d` at `index`"],["cosine_iter","returns an iterator that yields the values for a cosine window of `size` with the coefficients `a`, `b`, `c` and `d`"],["hamming_iter","returns an iterator that yields the values for a hamming window of `size`"],["hanning_iter","returns an iterator that yields the values for a hanning window of `size`"],["nuttall_iter","returns an iterator that yields the values for a nuttall window of `size`"],["triangular_at","returns the value of the [triangular window] (https://en.wikipedia.org/wiki/Window_function#Triangular_window) of `size` at `index`"],["triangular_iter","returns an iterator that yields the values for a triangular window if `l = size - 1` then the outermost values of the window are `0`. if `l = size` then the outermost values of the window are higher. if `l = size + 1` then the outermost values of the window are even higher."]],"struct":[["CosineWindowIter","holds the window coefficients and iteration state of an iterator for a cosine window"],["TriangularWindowIter","holds the iteration state of an iterator for a triangular window"]]};